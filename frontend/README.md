# Attendance Frontend

Minimal React (Vite) frontend.

Run:

1. cd frontend
2. npm install
3. npm run dev

Set API root by adding env: `VITE_API_ROOT=http://localhost:5000/api` if backend on default port.
